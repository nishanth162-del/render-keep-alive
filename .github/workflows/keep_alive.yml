name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Runs every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Render
        run: curl -X GET "https://your-render-app.onrender.com"  # Replace with your actual Render app URL
