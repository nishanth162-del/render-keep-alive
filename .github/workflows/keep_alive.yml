name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 7-23 * * *"  # Runs every 4 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Render
        run: curl -X GET "https://wallclubs.in/"  # Replace with your actual Render app URL
